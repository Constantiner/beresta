import { ALL, TRACE, DEBUG, INFO, WARN, ERROR, FATAL, OFF } from "./level/level";
import { getLogger, getRootLogger } from "./logger/logger";

export { ALL, TRACE, DEBUG, INFO, WARN, ERROR, FATAL, OFF, getLogger, getRootLogger };
